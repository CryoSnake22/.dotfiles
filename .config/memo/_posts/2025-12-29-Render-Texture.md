# Render Texture

Renders a camera onto a texture instead of the screen. good for stuff like security cameras, mirrors etc.

To get Pixelized look
Render Texture,

RAw image

Cam render to render texture

raw image show render texture

alt -> border stretch

Adjust dimensions in render texture

game -> no warn when no camera rendering
